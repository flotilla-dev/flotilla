[tool.poetry]
name = "flotilla-workspace"
version = "0.0.0"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<4.0"

flotilla-core = { path = "src/flotilla_core", develop = true }
flotilla-langchain = { path = "src/flotilla_langchain", develop = true }
